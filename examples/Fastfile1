asdf = 123
fastlane_version "2.0.0 #{asdf} #{asdf+232}"

before_all do
  git_pull
end

desc "Automatic Beta Deployment"
desc "Multiple lines"
lane :beta do
  sigh
  asd = "123"
  gym(scheme: "Example", force: true, some: asdf+1)
end

platform :ios do
  before_all do
    cocoapods
  end

  lane :appstore do
    slack(something: true, more: asdf+2)
  end

  after_all do
    slack
  end

  error do
    slack(success: false)
  end
end

platform :android do
  lane :play do
    gradle(task: "yolo")
  end
end
