before_all do
  git_pull
end

desc "Automatic Beta Deployment"
desc "Multiple lines"
lane :beta do
  sigh
  gym(scheme: "Example", force: true)
end

platform :ios do
  before_all do
    cocoapods
  end

  lane :appstore do
    slack(something: true)
  end

  after_all do
    slack
  end

  error do
    slack(success: false)
  end
end

platform :android do
  lane :play do
    gradle(task: "yolo")
  end
end
