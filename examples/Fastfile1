fastlane_version "2.0.0"

before_all do
  git_pull(something: 123, something2: "value2")
  git_pull
end

desc "Automatic Beta Deployment"
desc "Multiple lines"
lane :beta do
  sigh

  10.times do
    yolo
  end

  if 10 + 10 == 100
    while something
      puts 'hi'
    end
  end

  gym(scheme: "Example", force: true)
end

